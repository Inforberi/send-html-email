# Скопируй в .env.local и заполни значения.

# Gmail: отправитель и App Password (обязательно для отправки)
# https://myaccount.google.com/apppasswords — создать после включения 2FA
GMAIL_USER=tatarchuk.a.s.1997@gmail.com
GMAIL_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# Доступ к /send и /api/send-email (если не задан — защита отключена, только для dev)
SEND_ACCESS_TOKEN=your-secret-token
